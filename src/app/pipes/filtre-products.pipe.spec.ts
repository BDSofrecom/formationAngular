import { FiltreProductsPipe } from './filtre-products.pipe';

describe('FiltreProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
